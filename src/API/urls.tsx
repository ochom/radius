import { API_ROOT } from "./config";

export const URLS = {
  ALL_ROLES: `${API_ROOT}staff-roles`,
  ONE_ROLE: `${API_ROOT}staff-roles/`,

  ALL_STAFFS: `${API_ROOT}staffs`,
  ONE_STAFF: `${API_ROOT}staffs/`,

  ALL_CLASSROOMS: `${API_ROOT}classes`,
  ONE_CLASSROOM: `${API_ROOT}classes/`,

  ALL_SESSIONS: `${API_ROOT}sessions`,
  ONE_SESSION: `${API_ROOT}sessions/`,
};
